# Capsule 捕获、编辑与录屏

本模块覆盖截图、滚动截图、精确选区、标注、美化、截后浮层、贴图、OCR/二维码、临时捕获历史和录屏。保持 RIMES 单进程与 macOS 13 部署目标，不包含 Studio 视频编辑或在线分享。

## 使用

- 从 Capsule 底栏相机或默认 `⌘⇧5` 打开捕获横条；`⌘⇧4` 直接框选。快捷键可在设置中更改；若 macOS 自带截图占用，需先取消对应系统绑定。横条采用仅图标按钮，悬停或辅助功能可读名称。左侧为区域、全屏、窗口、滚动、延时、取字、录屏，右侧为比例、画幅、重设画幅和关闭；Esc 关闭。右键横条可恢复最近关闭卡片、打开已有工程、进入捕获与贴图设置。
- 右侧画幅入口支持先框选、再拖动选区、回车确认截图；固定比例在拖动到屏幕边缘时也保持不变。成功确认后记住比例和尺寸，下次仅在鼠标所在屏幕居中恢复并可拖动。在另一块屏幕开始框选时，旧屏选区和待确认结果立即清除；一次截图任务只保留一个活动选区。重设画幅清除旧尺寸以重新框选；`⌘⇧4` 保留松手截图。
- 标签依次为最近、捕获、笔记、图片、视频、PDF、技能、密码。“最近”是剪贴板历史；“捕获”是临时资产；图片和视频是长期收藏。
- 区域选择支持冻结画面、十字线、像素放大镜、像素/点尺寸、固定尺寸/比例、延时和上次区域。区域与画幅截图先并行取得各屏干净背景，再显示选区蒙层，避免将选区边框拍入冻结背景。准备期间再次按 `⌘⇧5` 可取消；显示选区后 Esc 取消会作废迟到结果。首次授权时不提前覆盖系统授权提示，预检查失败仍由 ScreenCaptureKit 实际检测，不据此拒绝截图。日志分别记录蒙层、内容枚举和各屏画面就绪耗时。
- 窗口截图和窗口录屏使用屏幕上的悬停高亮、单击选择，不再使用窗口下拉列表；显示应用与窗口标题，重叠处优先选择最上层可捕获窗口，桌面空白不可选择，Esc 或右键取消，再按 `⌘⇧5` 也可退出。选择层保留点击命中，面板直接处理取消键；工具条在选择层接管焦点后才关闭。全屏直接捕获。捕获排除 RIMES 自身窗口。
- 结果卡片悬停后显示中央复制／保存图标，角落为关闭、贴图、编辑（视频为播放）；缩略图可拖出。本版移除卡片收藏、更多和云上传入口。点击复制并成功写入剪贴板后，立即移除该卡片，其余卡片自动补位；复制失败保留卡片，历史列表与编辑器的复制行为不变。移除或关闭浮层保留历史，可恢复最近关闭项。位置、大小、自动关闭在捕获设置中调整。
- 结果堆栈在同一屏幕沿边排列，最新一张靠近底角；刷新内容保留窗口身份、顺序和关闭计时。截图期间隐藏，结束后统一恢复布局。超出屏幕容量的较旧卡片暂时隐藏，关闭可见卡片后补位；资产仍保留在捕获历史。缩略图按完整图片等比缩放，加载失败显示提示。
- 延时截图显示倒计时，Esc、取消按钮或再次按截图快捷键均可取消；不会留下在后台继续执行的等待任务。若 AppKit 枚举的某个屏幕不可由 ScreenCaptureKit 捕获，仅移除该屏蒙层，其他可捕获屏幕继续工作。
- 锁屏、睡眠、会话失活和安全输入分别门控：任一状态未解除都不恢复内容。保护会取消待截图任务，全部解除后恢复仍存活且原先可见的编辑／贴图／文字窗口及结果堆栈；不复活已关闭窗口，不自动恢复录制。
- 贴图可调整窗口大小、透明度，点击后用方向键移动，Shift 加速；锁定穿透后从捕获设置解锁或关闭所有贴图。锁屏隐藏内容。
- OCR 结果可复制、存笔记或显式送入 Buffer；不会直接向宿主输入框发送。导入的 Buffer 块要求显式投递，即使已有自动上屏偏好也不会由 OCR 导入启动投递。

### 自定义窗口圆角

截图结果卡片、编辑器、贴图及捕获辅助窗口与 Capsule 管理窗口使用透明窗口底板，背景和完整内容树在同一圆角内裁剪，避免浅色系统外观下露出矩形底色。替换内容、调整窗口尺寸或切换主题后仍保留裁剪。Capsule 列表与编辑区域同时裁剪滚动内容；Capsule 底栏、Buffer 和卡片悬停毛玻璃另设可伸缩材质遮罩，避免系统背景材质越过圆角。

捕获横条保留两组之间的透明间隙；全屏选区蒙层覆盖到屏幕四角，不做圆角裁剪。设置、Mailbox 等使用系统标准标题栏的窗口继续由 AppKit 管理外形，不改变焦点、拖动与缩放行为。

### 授权与检测

显式截图通过 ScreenCaptureKit 取得可捕获内容，不再仅因 CoreGraphics 预检查失败就阻断。系统实际拒绝当前进程时，截图入口与「设置 → 系统权限」共用一个授权引导：请求授权、打开对应系统面板、检测并继续；三项为独立用户操作，不自动连弹错误或跳转。返回应用只更新预检查提示，不自动截图。只将 ScreenCaptureKit 的 `userDeclined` 归为权限问题，服务失败、超时等保留实际错误。

“已勾选”不保证当前进程立即可用，授权后可能需要重启。临时签名开发版每次重建会改变代码身份，旧授权可能仍显示开启却不再匹配；先检测、保存未完成工作并重启，仍失败时在系统设置移除旧 RIMES、添加当前安装路径。稳定的 Apple Development 签名有助于保留开发授权；应用不修改 TCC 数据库、不自动重置权限或重启。页面的「重置辅助功能授权…」仅处理辅助功能，并需单独确认，不能用来修复屏幕录制。

找不到应用或从权限列表移除后，可在引导和「设置 → 系统权限」的应用卡片中拖动 RIMES 图标到列表。卡片传递当前运行 `.app` 的真实文件 URL，只允许复制语义，不移动或删除输入法；不从 Spotlight 猜测另一份同名应用。也可点「在 Finder 中显示」选中应用，或「复制应用路径」，在系统权限列表点「＋」后按 `⇧⌘G` 粘贴定位；应用仍留在 Input Methods 安装目录，不必搬到 Applications。手动添加仅面向屏幕录制、辅助功能和输入监控，摄像头与麦克风仍须显式请求。应用不存在或裸 SwiftPM 调试进程不提供可操作的应用卡片。

录屏只为启用的摄像头、非 GIF 麦克风和全局按键显示请求额外权限，等待用户回答后才能继续。关闭授权引导或进入会话保护会作废在途回调。查看历史、编辑已有截图和贴图不要求屏幕权限。

## 编辑工程

编辑器采用独立深色原生窗口，保留系统红黄绿按钮。顶部单排胶囊工具，中央为完整画布；底部只保留缩放、撤销／重做、拖出图片、效果预览、贴图和复制。删除常驻左右侧栏、底部历史栏和重复保存入口；历史仍在 Capsule 的“捕获”中，不删除任何资产。

可见工具包括裁剪、选择、空心／实心矩形、椭圆、线条、箭头、文字、像素化、聚光灯、编号、画笔和文字高亮。弯箭头、模糊等旧工程数据仍可渲染，本版不再占用工具栏。选择工具可移动标注和图层、双击编辑文字，Delete 删除。可导入／拖入图片；撤销重做支持按钮、⌘Z／⌘⇧Z。缩放支持适合、50%、100%、200%，放大时滚动平移画布。

仅点选或零位移不会新增撤销步骤，也不会清除重做；一次连续拖动作为一个撤销步骤。历史时长对非法、非有限或溢出元数据防御处理，避免损坏的录屏影响整个历史栏。

标注颜色使用弹出色板：饱和度／亮度、色相、透明度、Hex、RGBA、屏幕取色和十个自存色。六位 RGB 工程兼容保留，带透明度的新颜色写为八位 RGBA；透明度会与高亮效果相乘。选中标注可改色和线宽，一次色板调整合并为一个撤销步骤。背景与画布收进顶部按钮弹出层，提供纯色、留白、圆角、阴影、旋转和翻转；旧工程的其他效果不主动清除。眼睛按钮切换标注原始画布／最终效果。

⌘S 保存工程；“完成”保存后关闭，“另存为…”导出 PNG／JPEG。已收藏资产更新原条目，未收藏资产保持临时历史。复制、导出和拖出均使用当前渲染结果，包括未保存的遮盖。旧 `.rimesproject` 仍可从横条右键菜单打开；本版不在编辑器显示工程导出、收藏或云上传按钮。

## 滚动与录屏

滚动捕获支持横向、纵向及手动/自动滚动，显示累积拼接预览。自动滚动单独要求辅助功能授权。匹配忽略边缘固定区域、细化到原始像素，并去除可识别的固定尾栏重复。空白、重复或无法可靠对齐的内容会暂停，保留现有部分并标记未完成。上限为最长边 32,768 像素和总计 6,400 万像素；虚拟列表或动态页面可能需要分段捕获。

录屏可选择区域、窗口或全屏，倒计时、暂停/继续，MP4/H.264 或 GIF；提供画质、帧率、分辨率、光标、系统声、麦克风、摄像头、点击和按键样式选项。初始为 MP4、1080p 上限、30 FPS；系统声、麦克风、摄像头、点击和按键均关闭，后续记住选择。双音源输出混合为一条音轨。摄像头可设置位置、大小、形状及全屏。

GIF 明确不含音轨，转码限制为 2 分钟、15 FPS 和 1280 像素，超限保留 MP4 并提示。录后仅播放/保存/拖出/收藏。MP4 和 GIF 录制均收入视频收藏，不参与 iCloud 同步。

桌面清理通过窗口过滤与纯色合成，不修改 Finder 设置。勿扰联动运行用户填写的系统快捷指令，设置按钮打开 Shortcuts。未配置时不改变系统专注状态。

屏幕、麦克风、摄像头权限只在使用时请求；全局按键显示单独检查输入监控及辅助功能。拒绝授权时提示关闭对应选项继续。保护输入暂停录制并清除按键显示；锁屏、睡眠和会话退出停止采集并尝试封装。设备断开、编码失败会尝试保留片段并标记未完成。

## 资产与同步

默认目录是 Capsule 本地根目录下的 `captures/`。SQLite 只保存索引，UUID 子目录包含不可变原图/视频、版本化工程、当前渲染和原子恢复清单。写入媒体和工程完成后才发布记录；启动时从清单修复索引、回收删除事务，并恢复可播放的未完成 MP4。不完整 MP4 保留原件，不假装恢复成功。

临时历史保留 30 天、最多 10 GB，清理最早未收藏且无使用租约的资产；编辑、浮层、贴图、文件导出和文件承诺拖拽有租约。收藏与历史共享资产身份，重复收藏不复制媒体。现有 Markdown 和旧图片路径继续可读。

图片同步使用现有开关和容量限制，先写图片及自包含工程依赖，再发布条目；依赖不完整会延后或报错。工程原始素材上限 180 MB，归档上限 256 MB。视频没有同步开关。

## 验证

构建使用与本机 SDK 配套的 Apple Swift（完整 Xcode 或当前 Command Line Tools）：

```sh
swift build -c debug
.build/debug/RimeBuffer capture-smoke
.build/debug/RimeBuffer capture-chrome-smoke .build/capture-validation/chrome
.build/debug/RimeBuffer window-chrome-smoke .build/capture-validation/corners
.build/debug/RimeBuffer capture-selection-smoke
.build/debug/RimeBuffer capture-permission-smoke
.build/debug/RimeBuffer capsule-rail-smoke
.build/debug/RimeBuffer capsule-window-smoke
.build/debug/RimeBuffer capsule-sync-smoke
.build/debug/RimeBuffer capsule-password-smoke
.build/debug/RimeBuffer clipboard-history-smoke
.build/debug/RimeBuffer clipboard-policy-smoke
.build/debug/RimeBuffer buffer-window-smoke
.build/debug/RimeBuffer settings-routing-smoke
.build/debug/RimeBuffer theme-appkit-smoke
.build/debug/RimeBuffer buffer-capture-smoke
```

`capture-smoke` 验证清理租约、收藏身份、原图不可变、遮盖像素、工程导入/恢复、跨设备依赖完整性、横纵拼接、尺寸变换及视频本机策略。`capture-media-smoke <双音轨测试 MP4>` 验证混音、H.264 与动态 GIF。`capture-live-smoke 1800` 只录制自身测试窗口并订阅系统音频，包含暂停/继续；它记录主循环定时器偏差，不能替代真实中文输入延迟测量。

`capture-chrome-smoke` 使用隔离素材验证紧凑编辑器布局、色板真实弹出、像素缩放、RGBA 渲染、非法 Hex 回退、自存色去重、捕获模式路由，并生成原生卡片／编辑器／色板／横条截图。视觉对照与已知范围记在 `design-qa.md`。

`window-chrome-smoke` 已纳入 CI：用临时 Capsule 库和 AppKit 测试窗口检查浅色／深色、1×／2× 四角像素透明度、满幅子视图与悬停层裁剪、管理窗口缩放、内容视图替换、材质遮罩及全屏蒙层例外；可选输出目录用于图像检查。不修改用户主题偏好、真实内容库、剪贴板或系统权限。

`capture-smoke` 还用临时素材和隔离存储验证三张真实结果窗口：重复刷新不重建、不重叠，隐藏期间刷新不显示，恢复及关闭后补位，元数据刷新不取消缩略图，缺失缩略图回退当前渲染版本，过期回调不覆盖或复活窗口；纯布局测试覆盖左右侧、负坐标及屏幕容量溢出。不读取或修改用户截图。

合并前回归另覆盖点选后的撤销／重做、标注与图层连续拖动、重叠保护原因、保护中关闭窗口、倒计时取消和迟到任务、缺失部分／全部显示器、左侧默认值、非法时长。窗口命中统一使用 `kCGWindowBounds` 与 `CGDisplayBounds` 的全局左上角坐标，只减显示器原点；测试包含负坐标屏幕与原生 AppKit 翻转视图换算。若系统允许读取本进程测试窗口的 WindowServer 元数据，再验证该边界；不可用时明确报告，不触发权限请求或读取屏幕像素。CI 的 smoke 列表包含捕获与权限测试；真实锁屏通知顺序、外部窗口截图和硬件显示器热插拔仍需人工验收。

复制回归使用独立 pasteboard 验证成功后移除、保留历史与文件、恢复卡片、其他入口复制不关闭卡片、剪贴板竞争及素材不可用时保留卡片、迟到复制不关闭新恢复的卡片；不改写用户剪贴板。`capture-selection-smoke` 用合成图片与本地 AppKit 鼠标事件验证画面未就绪时框选、迟到画面交付一次、Esc 取消、固定尺寸/比例、窗口重叠与跨屏坐标命中，不触发真实屏幕权限或录制；可传预览目录生成窗口选择器 PNG。真实快捷键耗时与外部窗口捕获仍需安装后实测。

`capture-permission-smoke` 使用假系统结果覆盖权限/非权限错误分类、等待授权答案、重复点击、取消后的迟到回调、拒绝后重试及按选项请求；另以临时应用包和独立 pasteboard 验证应用身份、含空格/中文路径的原生文件拖动、仅复制操作和失效路径拒绝，不触发真实授权或改写用户剪贴板。`capture-permission-preview-smoke <PNG路径>` 渲染原生引导，所有授权、设置跳转、检测、应用拖动/定位/复制与重启动作禁用。真实系统授权和系统设置接受拖入仍需安装后人工验证。

真实验收必须另外覆盖混合 Retina/负坐标多屏、不同 Space、四类真实滚动内容、摄像头/麦克风设备和权限撤销、锁屏睡眠、宿主复制拖拽粘贴及中文输入。编译与 smoke 通过不代表这些硬件和真实宿主场景已经全部验收。

安装会重启输入法，保留词库并显式选择本机 Apple Swift，避免脚本选到不匹配 SDK 的 Homebrew 工具链。开发安装还需传入已经验证的稳定签名身份：

```sh
RB_KEEP_USERDB=1 \
RB_SWIFT_BIN=/usr/bin/swift \
RIMES_SIGN_IDENTITY='<本机有效的 Apple Development 身份>' \
./build_install.sh release
```

随后核对安装 bundle、签名、进程和输入源。代码完成、已安装、真实交互验证分别记录，不互相替代。

## 本轮交付记录（2026-09-21）

- 四张参考图对应的堆栈卡片、编辑器、弹出色板和捕获横条已实现；原生视觉检查见 `design-qa.md`。卡片复制成功后移除、快速蒙层、窗口悬停选择也包含在本轮构建。
- Apple Swift 6.3.3 / SDK 26.5：Debug 与 Release 构建通过；部署目标 macOS 13。Release 优化编译耗时约 310 秒。
- `capture-smoke`（包含 overlay / selection）、`capture-chrome-smoke`、权限、剪贴板策略／历史、Capsule rail／window 回归通过。真多屏与录屏设备全链路仍不据此标记已验收。
- 已通过 `RB_KEEP_USERDB=1` 构建并安装至 `~/Library/Input Methods/RIMES.app`，保留词库、配置与历史；稳定 Apple Development 签名，Team `585J2TL9U6`，严格深度验签通过。
- 安装与 Release 二进制 UUID 同为 `3ACA1FD0-970D-3909-9B6F-0A88336A411E`。构建号 `1789975030`（2026-09-21 15:17:10 CST），新单实例 PID `43291`，输入源注册、启用和选择均验证成功。版本标签仍为 `0.5.4-3-gba1fcb8-dirty`，不能只根据该标签判断本次是否更新。
- 安装后 UI 自动化按路径及 bundle ID 附着后台输入法均超时；Finder 的快捷键发送未提供可确认的 RIMES 窗口结果。因此真实快捷键延迟／真实外部窗口选择仍留给人工验收，未宣称已实测通过。
- 未提交或推送；既有工作区改动保持。

## 历史交付记录（2026-09-20，非本轮构建结果）

当前为已安装的捕获体验版；10 类能力已接入，完整实机验收仍有待完成项。

| 证据层次 | 结果与范围 |
| --- | --- |
| 构建 | 完整 Xcode 的 Debug / Release 构建通过；部署目标保持 macOS 13。尚未在 macOS 13 真机上运行验证。 |
| 自动回归 | 上述 11 项 smoke 通过；涵盖资产租约与清理、收藏身份、遮盖像素、工程与同步依赖、拼接、Capsule/剪贴板/Buffer/主题回归。 |
| 媒体 | 双音轨合成为单音轨、H.264 与动态 GIF 测试通过。 |
| 长录 | 独立调试程序只录自身测试窗口，订阅系统音频并执行暂停/继续，封装成功，输出约 1,875 秒；进程常驻内存约 52 MB，主循环定时器最大偏差约 68 ms。此测试先于最后的效果刷新与资源释放收尾；最终版本另通过 18 秒短录复测。没有据此确认真实中文按键延迟或实际音频听感。 |
| 视觉 | 原生编辑器在墨竹、翡翠与静谧主题下完成截图检查；图中使用独立测试素材。最终视觉与拖拽手感仍需真实工作流验收。 |
| 安装 | `RB_KEEP_USERDB=1` 保留数据安装成功；已安装与 Release 的 UUID 均为 `8F1C64BD-21F1-39FE-8CA3-7C2414162AD2`；深度签名验证通过、单一进程运行，当前输入源为 `com.scholay.inputmethod.isaac.Hans`。 |
| 工作区 | 原先 Capsule 搜索、光标和输入路由修改保留；对照基线的 238 行新增内容全部仍在。未创建提交或 PR。 |

本次生成的日志、安装核对 JSON 及原生界面截图在 `.build/capture-validation/`，属于本机验证产物，不进入用户资料目录或版本库。

尚未通过完整验收的场景包括：混合 Retina 与负坐标多屏、跨 Space、浏览器/PDF/代码/聊天真实滚动内容、摄像头与麦克风实机及设备断开、磁盘不足与权限撤销、锁屏睡眠、真实宿主的复制/拖拽/粘贴和中文输入延迟。当前 UI 自动化未能可靠附着后台输入法，TextEdit 合成按键也未提供有效中文组字证据；这些项目保持待验收，不标记为通过。
